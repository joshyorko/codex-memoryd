# Issues 212-216 Regression Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore deterministic Dreamer fixtures, hermetic runtime/path CLI behavior, and assertions aligned with durable-record/public-handle contracts for issues #212-#216.

**Architecture:** Keep Dreamer decay based on the explicit evaluation timestamp and use controlled evidence timestamps in the fixture. Route explicitly selected runtime status to the managed-runtime reporter, while retaining explicit URL daemon status. Make path inventory use optional config loading so inspection does not validate a missing file or create one. Keep recall/search public handles unchanged and update stale raw-ID assertions to verify handle identity plus durable provenance.

**Tech Stack:** Rust, Cargo integration tests, SQLite/FTS5, clap CLI, assert_cmd.

## Global Constraints

- Do not contact, stop, restart, reconfigure, or write to the daemon at `127.0.0.1:8787`.
- Do not access or modify `.dogfood`, `~/.codex/memories`, credentials, or private data.
- Preserve loopback-only defaults, explicit runtime selection, temporal filtering, archived recoverability, provenance, and recall-not-authority semantics.
- Work only on `agent/issues-212-216-test-regressions` from `origin/master` `6b54863b91fbddb22c2cc8a78d96b1c7f6b08901`.

---

### Task 1: Confirm regression contracts and update deterministic/identity fixtures

**Files:**
- Modify: `tests/dreamer.rs:289-350,553-576`
- Modify: `tests/cli_smoke.rs:1342-1425`

**Interfaces:** Existing `dream`, `turn`, recall, and search APIs. Public `mr_` handles remain the CLI/API identity; durable IDs are verified through existing provenance/archived fields.

- [ ] **Step 1: Write the failing assertions/fixtures.** Replace the #212 wall-clock conclusion fixture with controlled evidence at a timestamp exactly 30 days before `2026-07-14T00:00:00Z`. For #213 and #216 compare returned IDs to `ids::public_handle(PublicHandleKind::MemoryRef, durable_id)` and retain assertions for `archived`, temporal selection, history membership, and provenance.
- [ ] **Step 2: Run the four exact affected tests and confirm failures remain production/test-contract failures rather than fixture typos.**
- [ ] **Step 3: Apply only fixture/assertion corrections; do not change recall/search identity projection.
- [ ] **Step 4: Run the two Dreamer and two CLI exact tests; expect them to remain red only for #214/#215 until Tasks 2-3 are complete.

### Task 2: Make managed runtime status independent of the default daemon

**Files:**
- Modify: `src/cli.rs:1005-1020`
- Test: `tests/cli_smoke.rs:4490-4505`

**Interfaces:** `Cli::runtime_options`, `native_runtime::status`, and explicit `--url` daemon client behavior.

- [ ] **Step 1: Add/retain the hermetic regression test using `--runtime container` and a temporary home; it must assert the managed-container shape without an HTTP request to the default endpoint.
- [ ] **Step 2: Run the exact smoke test and observe the current daemon-routing failure.
- [ ] **Step 3: Route status to `native_runtime::status` when runtime is explicitly selected, while preserving local DB status and explicit `--url` daemon status precedence.
- [ ] **Step 4: Run the exact container status test and the focused explicit-URL status test.

### Task 3: Let path inventory inspect missing explicit config paths

**Files:**
- Modify: `src/cli.rs:2512-2550`
- Test: `tests/cli_smoke.rs:4578-4640`

**Interfaces:** `Config::load`, `RuntimeOptions`, `PathsInventory`, and operational commands that still call required config loading.

- [ ] **Step 1: Run the exact paths JSON test and observe required-config validation occurring before inventory rendering.
- [ ] **Step 2: Make `paths_inventory` use a non-required config resolution path (default config plus environment/CLI runtime overrides as applicable), without creating the selected config file.
- [ ] **Step 3: Keep `open_service` and other operational commands on required explicit-config validation, and assert the JSON key/value inventory remains deterministic.
- [ ] **Step 4: Run the exact paths test and the existing missing-config operational validation test.

### Task 4: Full verification and publish

**Files:** Only files changed by Tasks 1-3, plus this plan document if repository convention keeps implementation plans.

- [ ] **Step 1: Run all required exact tests, full `dreamer`, full `cli_smoke`, `dream_jobs`, `cargo fmt --check`, and `cargo check`.
- [ ] **Step 2: Repair any related regressions and rerun the affected checks.
- [ ] **Step 3: Inspect the final diff/status, stage only scoped files, and commit with a conventional fix message.
- [ ] **Step 4: Push the branch to `origin` and open a non-draft PR against `master` with root causes, exact results, and `Fixes #212` through `Fixes #216` closing lines.
- [ ] **Step 5: Report the PR URL, commit SHA, changed files, and validation results without merging.
