# MEMORY

- Repo convention: recall responses are tagged `authority = "recall_not_authority"`
  and never override current user instructions or repository files.
- Repo convention: the HTTP daemon binds loopback by default; non-loopback
  exposure reports `auth_missing` status.
